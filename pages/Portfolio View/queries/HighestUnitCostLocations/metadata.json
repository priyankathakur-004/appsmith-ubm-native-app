{
  "gitSyncId": "68f08f3128a92f6fcd2cb504_29219558-2889-43df-909b-c97030e40702",
  "id": "Portfolio View_HighestUnitCostLocations",
  "pluginId": "postgres-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "SELECT\n    l.name AS location_description,\n    SUM(\n        COALESCE(amf.total_charges_consumption, 0) + \n        COALESCE(amf.total_charges_generation, 0)\n    ) /\n    NULLIF(SUM(COALESCE(amf.total_consumption, 0)), 0) AS unit_cost\nFROM bill_management_v2.analytics_monthly_feed amf\nJOIN bill_management_v2.locations l\n    ON l.id = amf.location_id\nWHERE amf.customer_id = {{this.params.customer_id}}\n  {{ this.params.utility_type ? `AND amf.utility_type = '${this.params.utility_type}'` : '' }}\n  {{ this.params.date_start && this.params.date_end ? `AND amf.time_period BETWEEN '${this.params.date_start}' AND '${this.params.date_end}'` : '' }}\n\t{{ this.params.location_ids && this.params.location_ids.length ? `AND amf.location_id IN (${this.params.location_ids.join(',')})` : '' }}\n\nGROUP BY l.id, l.name\nHAVING SUM(COALESCE(amf.total_consumption, 0)) > 0\nORDER BY unit_cost DESC;\n",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": false
        }
      ],
      "timeoutInMillisecond": 1000000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "UBM_Database",
      "isAutoGenerated": false,
      "name": "UBM_Database",
      "pluginId": "postgres-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "HighestUnitCostLocations",
    "pageId": "Portfolio View",
    "runBehaviour": "MANUAL",
    "userSetOnLoad": true
  }
}